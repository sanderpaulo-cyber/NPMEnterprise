kind = "web"
previewPath = "/"
title = "NPM Enterprise Dashboard"
version = "1.0.0"
id = "artifacts/npm-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20112

[services.development]
run = "pnpm --filter @workspace/npm-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/npm-dashboard", "run", "build" ]
publicDir = "artifacts/npm-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20112"
BASE_PATH = "/"
